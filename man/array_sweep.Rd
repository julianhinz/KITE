% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{array_sweep}
\alias{array_sweep}
\title{Sweep Over Array Margins}
\usage{
array_sweep(x, MARGIN, STATS, FUN = "*")
}
\arguments{
\item{x}{Numeric array.}

\item{MARGIN}{Integer or character vector identifying the margins over which
`STATS` should be swept. Character margins are matched against the names of
`dimnames(x)`.}

\item{STATS}{Numeric array or vector that is broadcastable over `MARGIN`.}

\item{FUN}{Character scalar choosing the operation. Supported values are
`"+"`, `"-"`, `"*"`, and `"/"`.}
}
\value{
An array with the same shape (and dimnames) as `x` containing the
  swept result.
}
\description{
Applies an element-wise operation between an array and the supplied
statistics broadcast over the requested margins while preserving dimension
names. Matches the semantics of [base::sweep()].
}
\examples{
x <- array(1:12, dim = c(3, 4))
dimnames(x) <- list(country = c("A", "B", "C"), sector = c("X", "Y", "Z", "W"))
stats <- c(1, 2, 3)
array_sweep(x, MARGIN = "country", STATS = stats, FUN = "-")
}
